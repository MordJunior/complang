neg(i : int) : int
  if i < 0 then
    return i;
  else
    return 0 - i;
  end;
end;

foo(a : int, b : int) : int
  if a < neg(b) or not b < neg(a) then
    return a - b;
  else
    return b - a;
  end;
end;
