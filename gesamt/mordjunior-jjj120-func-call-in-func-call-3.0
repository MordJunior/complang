add(x: int, y: int): int
    return x + y;
end;

mul(x: int, y: int): int 
    return x * y;
end;

foo(a: int, b: int, c: int): int
    return add(mul(c, add(b, mul(a, add(c, mul(b, a))))), mul(c, add(b, mul(a, add(c, mul(b, a))))));
end;
