add(x: int, y: int): int
    return x + y;
end;

mul(x: int, y: int): int 
    return x * y;
end;

foo(a: int, b: int, c: int): int 
    return mul(add(mul(add(mul(a, b), c), a), b), c) + mul(add(mul(add(mul(a, b), c), a), b), c);
end;
